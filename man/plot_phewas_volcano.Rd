% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano.R
\name{plot_phewas_volcano}
\alias{plot_phewas_volcano}
\title{Plot a PheWAS volcano result}
\usage{
plot_phewas_volcano(
  x,
  significance = "bonferroni",
  label = NULL,
  highlight = NULL,
  category_order = NULL,
  x_limit = NULL,
  y_limit = NULL,
  missing = c("error", "drop"),
  title = NULL,
  label_mode = c("description", "phenotype", "both"),
  top_label_n = 0L,
  label_wrap_width = 26L,
  group_legend_columns = 5L,
  group_wrap_width = 24L,
  point_size = 2.2,
  base_size = 12,
  label_size = base_size
)
}
\arguments{
\item{x}{A \code{phewas_result}.}

\item{significance}{Testing threshold to draw: \code{"bonferroni"}, \code{"bh"}, or
\code{"none"}.}

\item{label, highlight}{Explicit phenotype values to label or ring.}

\item{category_order}{Optional character vector giving every plotted group
in display order.}

\item{x_limit, y_limit}{Optional reviewed axis limits. Limits that clip data
or the testing threshold error. \code{x_limit} must be symmetric around zero.}

\item{missing}{What to do when selected phenotypes are unavailable.}

\item{title}{Optional in-plot title.}

\item{label_mode}{Label text: phenotype \code{description}, \code{phenotype}, or
\code{"both"}.}

\item{top_label_n}{Number of strongest associations to label automatically
when \code{label} is \code{NULL}. Use zero to disable automatic labels.}

\item{label_wrap_width}{Approximate characters per label line.}

\item{group_legend_columns}{Number of columns in the bottom group legend.}

\item{group_wrap_width}{Approximate characters per group-label line.}

\item{point_size}{Point size in millimetres.}

\item{base_size, label_size}{Base and association-label font sizes in points.}
}
\value{
A \code{ggplot} object with a \code{phewas_audit} attribute.
}
\description{
Plots fitted coefficients against the authoritative stored \code{-log10(p)}
values using the same group legend, colors, effect-direction symbols, theme,
and annotation style as \code{\link[=plot_phewas_manhattan]{plot_phewas_manhattan()}}. Coefficients remain on
their fitted model or link scale. Results containing different effect
measures are separated into free-scale facets, each centered at zero,
because their magnitudes are not directly comparable. Mean differences also
require common outcome units and transformations before their magnitudes can
be compared within a panel.
}
\details{
The default Bonferroni threshold uses the recorded original
successful testing family, including successful rows omitted from the plot
because their estimate is not finite. Threshold text is placed inside the
plotting panel rather than added as an axis tick.
}
