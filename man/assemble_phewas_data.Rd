% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{assemble_phewas_data}
\alias{assemble_phewas_data}
\title{Assemble analysis-ready PheWAS data}
\usage{
assemble_phewas_data(
  sample,
  phenotypes = NULL,
  anchors = NULL,
  covariates = NULL,
  id
)
}
\arguments{
\item{sample}{Data frame defining the people and row order in the analysis.}

\item{phenotypes}{Optional data frame of analysis-ready phenotypes.}

\item{anchors}{Optional data frame of fixed predictors or outcomes.}

\item{covariates}{Optional data frame of adjustment variables.}

\item{id}{Name of the ID column shared by every supplied table.}
}
\value{
A \code{data.table} in sample-spine order. Its \code{join_report} attribute
reports matches and unmatched rows for every input table.
}
\description{
Strictly left-join phenotype, anchor, and covariate tables to a declared
sample spine. Every input must contain one unique, nonmissing ID per row.
Non-ID column collisions are errors, and the input objects are never
modified by reference.
}
\details{
Input data tables are copied before any work is done, so this
function never changes caller-owned objects by reference. Use
\code{\link[data.table:copy]{data.table::copy()}} before subsequently modifying the returned table with
\verb{:=}, or \code{as.data.frame()} when base-data-frame semantics are preferred.
}
