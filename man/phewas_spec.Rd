% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{phewas_spec}
\alias{phewas_spec}
\title{Define a PheWAS analysis specification}
\usage{
phewas_spec(
  analysis_id,
  anchor,
  direction = c("phenotypes_as_outcomes", "phenotypes_as_predictors"),
  phenotypes,
  covariates = character(),
  outcome_type = NULL,
  anchor_type = NULL,
  anchor_transform = c("none", "zscore"),
  anchor_reference = NULL,
  anchor_levels = NULL,
  anchor_scores = NULL,
  offset = NULL,
  exclusion_column = NULL,
  exclusion_values = NULL,
  eligibility,
  fdr_threshold,
  version = "1.0"
)
}
\arguments{
\item{analysis_id}{Stable, unique name for the analysis.}

\item{anchor}{Name of the fixed predictor (phenotypes as outcomes) or fixed
outcome (phenotypes as predictors).}

\item{direction}{Either \code{"phenotypes_as_outcomes"} or
\code{"phenotypes_as_predictors"}.}

\item{phenotypes}{Data frame with phenotype metadata. Required columns are
\code{phenotype}, \code{description}, \code{group}, \code{groupnum}, and \code{color}. Missing or
blank \code{variable_type} values default to \code{"binary"}; binary phenotypes with
a missing or blank \code{reference} default to \code{"0"}. Outcome scans additionally
require \code{outcome_type}.}

\item{covariates}{Character vector of adjustment-variable columns.}

\item{outcome_type}{Fixed-anchor outcome type for phenotype-predictor scans.}

\item{anchor_type}{Fixed-anchor type. Required for phenotype-outcome scans.}

\item{anchor_transform}{Either \code{"none"} or \code{"zscore"}.}

\item{anchor_reference}{Explicit reference for a binary anchor.}

\item{anchor_levels}{Explicit ordered levels for an ordinal anchor.}

\item{anchor_scores}{Optional strictly increasing ordinal-trend scores.}

\item{offset}{Optional log-offset column for a fixed count outcome.}

\item{exclusion_column}{Optional cohort marker column. Matching rows are
removed before base-cohort completeness and anchor transformation.}

\item{exclusion_values}{Explicit marker values to remove. Missing marker
values are retained.}

\item{eligibility}{Explicit named list containing \code{min_n}, \code{min_cases},
\code{min_controls}, and \code{min_outcome_levels}. \code{min_cases} and \code{min_controls}
set the minimum non-reference and reference counts for each scanned binary
phenotype, whether it is modeled as an outcome or predictor. They also
protect event and nonevent counts when the fixed outcome is binary.}

\item{fdr_threshold}{Threshold used to flag BH-adjusted results.}

\item{version}{Specification schema version.}
}
\value{
A validated object of class \code{phewas_spec}.
}
\description{
A specification describes exactly one fixed anchor and one family of
phenotype tests. Metadata may use native list-columns for \code{levels} and
\code{scores}, or \code{|}-delimited strings when it comes from TSV/YAML files.
The canonical metadata vocabulary is \code{phenotype}, \code{description}, \code{group},
\code{groupnum}, and \code{color}; \code{phenotype} names the analysis-data column. Legacy
\code{phenotype_id}/\code{phecode}/\code{phenotype_column}/\code{column}, \code{label}, \code{category},
and \code{category_order} fields are accepted only when they agree with any
canonical fields supplied in the same table, and are not retained in the
validated specification.
}
