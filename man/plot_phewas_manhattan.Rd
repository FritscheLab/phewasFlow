% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_phewas_manhattan}
\alias{plot_phewas_manhattan}
\title{Plot a PheWAS Manhattan-style result}
\usage{
plot_phewas_manhattan(
  x,
  significance = "bonferroni",
  label = NULL,
  highlight = NULL,
  category_order = NULL,
  y_limit = NULL,
  missing = c("error", "drop"),
  title = NULL,
  label_mode = c("description", "phenotype", "both"),
  top_label_n = 0L,
  label_wrap_width = 26L,
  group_display = c("legend", "auto", "x_axis"),
  group_legend_columns = 5L,
  group_label_rows = 3L,
  group_wrap_width = 24L,
  point_size = 2.2,
  base_size = 12,
  label_size = base_size,
  axis_break_mode = c("off", "auto", "on"),
  axis_break_top = 55,
  axis_break_top_size = 0.125
)
}
\arguments{
\item{x}{A \code{phewas_result}.}

\item{significance}{Any of \code{"bh"}, \code{"bonferroni"}, and \code{"none"}.}

\item{label, highlight}{Explicit phenotype values to label or ring.}

\item{category_order}{Optional character vector giving every group in
display order. The argument name is retained for API compatibility;
plotted metadata use the canonical \code{group} field.}

\item{y_limit}{Optional reviewed limits on the original \code{-log10(p)} scale.
Limits that clip data or testing thresholds error.}

\item{missing}{What to do when selected phenotypes are unavailable.}

\item{title}{Optional in-plot title. \code{NULL} leaves room for an external
report or slide title.}

\item{label_mode}{Label text: phenotype \code{description}, \code{phenotype}, or
\code{"both"}.}

\item{top_label_n}{Number of automatically selected labels when \code{label} is
\code{NULL}. Significant associations rank first; use zero to disable.}

\item{label_wrap_width}{Approximate characters per label line.}

\item{group_display}{Where to show phenotype groups. The default \code{"legend"}
matches the volcano plot and leaves a numeric phenotype index on the
Manhattan x-axis. \code{"auto"} uses centered group labels for compact group
sets and a legend for crowded group sets; \code{"x_axis"} always places group
labels on the Manhattan x-axis.}

\item{group_legend_columns}{Number of columns in the group legend.}

\item{group_label_rows}{Rows used to dodge group labels on the x-axis.}

\item{group_wrap_width}{Approximate characters per group-label line.}

\item{point_size}{Point size in millimetres.}

\item{base_size, label_size}{Base and association-label font sizes in points.}

\item{axis_break_mode}{Optional upper-tail compression: \code{"off"}, \code{"auto"},
or \code{"on"}.}

\item{axis_break_top}{Compression threshold on the original \code{-log10(p)}
scale.}

\item{axis_break_top_size}{Fraction of the plotting range allotted above
the compression threshold.}
}
\value{
A \code{ggplot} object with a \code{phewas_audit} attribute.
}
\description{
Uses canonical group colors, effect-direction triangles, group separators,
directly annotated testing thresholds, boxed labels, and bottom legends.
Stored \code{neg_log10_p} values remain authoritative, so probabilities that
underflowed to zero are still plottable. Group and phenotype order are
deterministic.
}
